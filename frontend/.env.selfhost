# 同源部署（Docker / 裸机 Node）的前端构建变量
#
# 由 `npm run build:selfhost`（vite build --mode selfhost）加载。Cloudflare 那条链路走的是
# 默认 production 模式、读不到本文件，改由部署工作流直接传 VITE_API_BASE_URL=/ ——
# 值和这里一样，因为前端现在也是由同一个 Worker 同源发出的。
#
# 前后端同源：把基准地址设成 "/"，App.tsx 里 `storedBackend.replace(/\/$/, "")` 会把它
# 削成空串，请求最终是 /api/xxx 这样的相对路径 —— 既不用配 CORS，也不用在产物里
# 烘焙具体域名。
VITE_API_BASE_URL=/

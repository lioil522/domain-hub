# 静态资源响应头配置（Vite 会把 public/ 原样拷进 dist/）
#
# Cloudflare 的静态资源托管原生认这个文件（Workers assets 与 Pages 用的是同一套语义）。
# 默认给所有文件下发 Cache-Control: public, max-age=0, must-revalidate，
# 于是每次刷新页面，浏览器都要为每个资源发一次条件请求等 304 —— 即使文件名里
# 已经带了内容哈希、内容根本不可能变。首屏三个资源就是三个白等的往返。
#
# Vite 产物中 /assets/ 下的文件名都带内容哈希（如 index-BibuQwET.js），
# 内容一变文件名就变，因此可以安全地设为 immutable 长缓存。

/assets/*
  Cache-Control: public, max-age=31536000, immutable

# index.html 必须保持每次校验，否则新版本发布后用户会一直拿到旧的
# HTML、继续引用已被删除的旧 hash 资源。

/index.html
  Cache-Control: public, max-age=0, must-revalidate

# theme-init.js 不带内容哈希，每次校验，避免发新版后浏览器拿旧脚本决定主题。

/theme-init.js
  Cache-Control: public, max-age=0, must-revalidate

# 全站安全响应头（与 src/index.ts 的 SECURITY_HEADERS、server/static.ts 同步维护）。
# CSP 收紧 script-src 为 'self'，前端主题初始化脚本必须外置（见 /theme-init.js），
# 不可再往 index.html 写内联 <script>。

/*
  Content-Security-Policy: script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'
  X-Frame-Options: DENY
  Referrer-Policy: no-referrer
  X-Content-Type-Options: nosniff
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=()
